#!/bin/bash
#export HTTPS=true
#export SSL_CRT_FILE=./certs/iothub.crt
#export SSL_KEY_FILE=./certs/iothub.key
#npm run start

#you need to run 'npm build' before to optimize the app 
npx serve -s build